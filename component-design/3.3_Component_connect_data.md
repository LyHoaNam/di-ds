# Database Connectable component
## **Summary Table**

| Block                      | Description                                                                                      |
|----------------------------|--------------------------------------------------------------------------------------------------|
| functions (`F`)            | Feature-specific business logic                                                                  |
| Zustand State (`Z`)        | Local/global state management for feature                                                        |
| Action Handler (`AH`)      | Handles UI-driven actions and event dispatching                                                  |
| hook (`HK`)                | Custom hooks, central logic hub for state, actions, and side effects                             |
| static presentation (`SP`) | Stateless display components, render via props                                                   |
| Database Connectable (`DB`)| Data-driven components (Table, Form, Drawer), interact with backend/state                        |
| Common constants (`CC`)    | Shared constants for consistency                                                                 |
| Utility functions (`UF`)   | Generic helpers for formatting, validation, etc.                                                 |
| BackEnd (`BE`)             | External API/backend, source of persistent data and business rules                               |
```mermaid

flowchart TD
    subgraph ContractFeature["specific feature"]
            F["functions"]
            Z[(Zustand State)]
            AH["Action Handler<br>onSubmit(), onChange()..."]
            HK["hook"]
            F --> HK
            Z -- get --> HK
            HK -- OptimisticUpdates --> Z
            AH <--> HK
    end

    subgraph UI["UI"]
        SP["static presentation"]
        DB["Database Connectable"]
        ER[ErrorBoundary]
        SP -- events --> DB
        DB -- props --> SP
    end


    HK <-- ER --> DB

    subgraph SharedPart["Shared (common) part"]
        CC["Common constants"]
        UF["Utilitys functions"]
    end

    CC --> HK
    UF -. abstraction .-> HK

    subgraph BackendAPI[" "]
        BE[(BackEnd)]
    end

    BE --  API(get) -- Cache -- DataTransformer --> HK
    HK -- DataTransformer -- API(post) --> BE

    %% Style blocks
    classDef purpleBox fill:#e2d6f3,stroke:#bfaee9,color:#000000,font-weight:bold;
    classDef greenBox fill:#d7ecd8,stroke:#9dc8a4,color:#000000,font-weight:bold;
    classDef dbBox fill:#d7ecd8,stroke:#9dc8a4,color:#000000,font-weight:bold;
    classDef backendBox fill:#f6d6d6,stroke:#e9aeae,color:#000000,font-weight:bold;
    classDef sharedBox fill:#fff,stroke:#bbb,color:#000000,font-weight:bold;
    classDef uiBox fill:#fff,stroke:#bbb,color:#000000,font-weight:bold;
    classDef stateBox fill:#e2d6f3,stroke:#bfaee9,color:#000000,font-weight:bold;

    class F,AH,HK purpleBox;
    class Z stateBox;
    class SP dbBox;
    class DB greenBox;
    class BE backendBox;
    class CC,UF sharedBox;
    class UI uiBox;
    class SharedPart sharedBox;

```

## Sample
// ===========================================
// 1. SHARED (COMMON) PART   
// ===========================================  

// Common constants
```ts
export const COMMON_CONSTANTS = {
};
// Utility functions
export const utilityFunctions = {
  formatCurrency: (amount: number): string => {
    return `$${amount.toLocaleString()}`;
  },
  // ... other utility functions
};
```

// ===========================================
// 2. BACKEND API
// ===========================================


```ts
interface Contract {
}
export const backendAPI = {
  async getContracts(): Promise<Contract[]> {
    // Simulate API call
    return mockContracts;
  },
  // ....other API methods
}
```

// ===========================================
// 3. SPECIFIC FEATURE (CONTRACT FEATURE)
// ===========================================

**3.1 Store**
```ts
// Zustand State
export const useContractStore = create<ContractState>((set) => ({
    // logic zustand here
}))

```
3.2 **Action Handler**
```ts
export const contractActionHandler = {
  onFormSubmit: async (formData: any): Promise<void> => {
    // Handle form submission
    // Validate → API call → Update state
  },

  onFormChange: (field: string, value: any): void => {
    // Handle form field changes
    // Update formData in state
  },

  onContractSelect: (contract: Contract): void => {
    // Handle contract selection
    // Update selectedContract in state
  },

  onContractDelete: async (id: string): Promise<void> => {
    // Handle contract deletion
    // API call → Update contracts list
  },
};
```

**3.3 Custom Hook (Central Hub)**
```ts
export const useContractFeature = () => {
  const store = useContractStore();

  // Load data on mount
  useEffect(() => {
    loadContracts();
  }, []);

  const loadContracts = async () => {
    // Fetch contracts from API
    // Update store state
  };

  return {
    // State
    contracts: store.contracts,
    selectedContract: store.selectedContract,
    isLoading: store.isLoading,
    error: store.error,
    formData: store.formData,

    // Actions
    actions: contractActionHandler,

    // Functions
    functions: contractFunctions,

    // Utilities & Constants
    utils: utilityFunctions,
    constants: COMMON_CONSTANTS,
  };
};
```

// ===========================================
// 4. UI COMPONENTS
// ===========================================

**4.1 Static Presentation Components**
```tsx
export const ContractStatusBadge: React.FC<{ status: string }> = ({
  status,
}) => (
  <span className={`badge ${getStatusColor(status)}`}>
    {status.toUpperCase()}
  </span>
);
```

**4.2 Database Connectable Components**

```tsx
export const ContractTable: React.FC = () => {
  const { contracts, isLoading, actions } = useContractFeature();

  if (isLoading) {
    return <div className="loading">Loading contracts...</div>;
  }
  return (
    // 
  )
}
```
